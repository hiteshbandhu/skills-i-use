---
name: train-llms-locally
description: >
  Runs workflows to train a small LLM from scratch on a laptop — tokenizer, data,
  architecture, training loop, and nanochat-style hands-on paths. Use when the
  user wants local pre-training, learns fundamentals, or says "train LLM from
  scratch", "nanochat", "tokenizer", "pretraining loop".
---

# Train LLMs locally

Playbook from Angelos Perivolaropoulos (ElevenLabs workshop). [workflows.md](workflows.md).

```
├─ Environment (laptop vs Colab)              → A
├─ Size tokenizer + data to domain            → B
├─ Run training loop / checkpoints            → C
└─ Connect pretrain vs post-train path        → D
```

Source: `playlists/fundamentals-ai-engineer/`.
